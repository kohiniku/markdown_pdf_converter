// jest-dom provides custom matchers for DOM nodes
// jest-domはDOMノード向けのカスタムマッチャーを追加するユーティリティ
// Example assertion: expect(element).toHaveTextContent(/react/i)
// 例えば次のようなアサーションが書けるようになる
// See https://github.com/testing-library/jest-dom for details
// 詳細は https://github.com/testing-library/jest-dom を参照
import '@testing-library/jest-dom/vitest';
